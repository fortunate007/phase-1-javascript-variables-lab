// Define companyName as a constant variable set to 'Scuber'
const companyName = 'Scuber';

// Define mostProfitableNeighborhood as a variable set to 'Chelsea' using let
let mostProfitableNeighborhood = 'Chelsea';

// Define companyCeo as a variable set to 'Susan Smith' using let
let companyCeo = 'Susan Smith';

// Export variables if needed in a Node.js environment (not explicitly requested, but sometimes useful in testing setups)
module.exports = { companyName, mostProfitableNeighborhood, companyCeo };
